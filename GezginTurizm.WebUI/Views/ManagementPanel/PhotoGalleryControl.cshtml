@model List<PhotoGallery>
@{
    ViewData["Title"] = "PhotoGalleryControl";
    Layout = "~/Views/Shared/_ManagementLayout.cshtml";
}

<!-- ======= Breadcrumbs ======= -->
<section id="breadcrumbs" class="breadcrumbs">
    <div class="container">
        <div class="d-flex justify-content-between align-items-center">
            <h2>Foto-Galeri Fotoğrafları</h2>
        </div>
    </div>
</section>
<!-- End Breadcrumbs -->

<br />
<br />
@if (TempData["QaWbExRzTqfYGZAXaCAVrBA"] != null)
{
    <div class="alert alert-success" role="alert">
        @TempData["QaWbExRzTqfYGZAXaCAVrBA"]
    </div>
}
@if (TempData["qqewrqbqrwqtmqAAAADazdaRemNBENb"] != null)
{
    <div class="alert alert-danger" role="alert">
        @TempData["qqewrqbqrwqtmqAAAADazdaRemNBENb"]
    </div>
}
<div class="container  ml-5 mr-5">
    <table class="table table-striped table-hover">
        <thead>
            <tr>
                <th>Fotoğraf Önizleme</th>
                <th>Sil</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        <img src="~/img/recent-photos/@item.PhotoName" width="170" height="100" />
                    </td>
                    <td><a href="#myModal_@item.PhotoId" class="btn btn-outline-danger" data-toggle="modal" data-target="#myModal_@item.PhotoId"><i class="bi bi-trash"></i></a></td>
                </tr>
                <div class="modal fade" id="myModal_@item.PhotoId">
                    <div class="modal-dialog">
                        <div class="modal-content">
                            <!-- Modal Header -->
                            <div class="modal-header">
                                <h4 class="modal-title">Uyarı</h4>
                                <button type="button" class="close" data-dismiss="modal">&times;</button>
                            </div>
                            <!-- Modal body -->
                            <div class="modal-body">
                                Silme işlemini onaylıyor musun ?
                            </div>
                            <!-- Modal footer -->
                            <div class="modal-footer">
                                <a href=@Url.Action("PhotoGalleryControlDelete","ManagementPanel",new { id=@item.PhotoId , fileName=@item.PhotoName }) class="btn btn-danger">Sil</a>
                            </div>
                        </div>
                    </div>
                </div>
            }
            <tr><td colspan="3"><a class="btn btn-primary" asp-controller="ManagementPanel" asp-action="PhotoGalleryAdd">Yeni Foto-Galeri Fotoğrafı Ekle</a></td></tr>
        </tbody>
    </table>
</div>

